services:
  - type: web
    name: matundu-sms-backend
    runtime: node
    root: backend
    region: oregon
    buildCommand: npm install && npx prisma generate
    startCommand: npx prisma migrate deploy && npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        fromDatabase:
          name: matundu-db
          property: connectionString
      - key: REDIS_URL
        fromService:
          name: matundu-redis
          type: redis
          property: connectionString
      - key: JWT_SECRET
        generateValue: true
      - key: JWT_REFRESH_SECRET
        generateValue: true
      - key: CORS_ORIGIN
        value: https://matundu-frontend.pages.dev
      - key: AUTH_RATE_LIMIT_MAX
        value: "15"
      - key: FORCE_RESEED
        value: "true"
    plan: free

databases:
  - name: matundu-db
    databaseName: matundu_sms
    user: matundu_user
    plan: free
    region: oregon

  - name: matundu-redis
    type: redis
    plan: free
    region: oregon
